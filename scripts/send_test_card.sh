#!/usr/bin/env bash
set -euo pipefail
export PYTHONPATH=.
# Uses the bot command handler; it will send in whichever chat you run it from:
# open your Control Chat and type: !testbuttons
echo "Type !testbuttons in your control chat to send a test card."
